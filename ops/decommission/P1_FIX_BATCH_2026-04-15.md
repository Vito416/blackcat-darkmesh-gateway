# P1 Fix Batch Evidence - 2026-04-15

Scope: follow-up hardening batch for replay guard, resolver outage behavior, and AO registry fail-closed type guards.

## Completed in this batch

### P1-03 Gateway AO outage behavior drill hardening

- Added optional global AO-unavailable cache window in resolver path:
  - `GATEWAY_SITE_RESOLVE_GLOBAL_UNAVAILABLE_CACHE_TTL_MS`
  - implementation in `src/runtime/template/siteResolver.ts`
- Preserved existing host-level unavailable cache + circuit breaker behavior.
- Added regression test for cross-host outage dedupe:
  - `tests/template-site-resolver.test.ts`
  - case: "uses global unavailable cache across hosts when enabled"

### P1-04 AO registry write fail-closed type-guard audit

- Added fail-closed Site-Id guard before auth/handler route dispatch for write actions:
  - `RegisterSite`, `SetSiteRuntime`, `UpsertSiteRuntime`, `BindDomain`, `SetActiveVersion`, `GrantRole`
- Guard behavior:
  - rejects non-string Site-Id aliases
  - rejects conflicting aliases (`Site-Id` vs `siteId` etc.)
  - canonicalizes alias into `Site-Id` when safe
- Implementation:
  - `blackcat-darkmesh-ao/ao/registry/process.lua`
- Added regression coverage:
  - `blackcat-darkmesh-ao/scripts/verify/contracts.lua`

### Worker replay/token hardening (supports P1-01/P1-02)

- Replay guard now uses claim-marker ownership check before final replay lock write.
- Strict scoped token mode now enforces token-topology sanity:
  - missing non-target scoped tokens -> fail closed
  - duplicate scoped token values -> fail closed
- Implementation:
  - `workers/secrets-worker/src/index.ts`
  - `workers/secrets-worker/test/inbox.test.ts`

## Validation snapshot

- `blackcat-darkmesh-ao`: `scripts/verify/preflight.sh` -> pass
- `blackcat-darkmesh-ao`: `AUTH_REQUIRE_SIGNATURE=0 AUTH_REQUIRE_NONCE=0 AUTH_REQUIRE_TIMESTAMP=0 AUTH_REQUIRE_JWT=0 lua5.4 scripts/verify/contracts.lua` -> pass
- `workers/secrets-worker`: `npm test` -> pass
- `blackcat-darkmesh-write`: `RUN_SCHEMA_MANIFEST=1 bash scripts/verify/preflight.sh` -> pass
- `blackcat-darkmesh-gateway`: `npm run test -- tests/template-site-resolver.test.ts` -> pass

## Remaining P1 runbook debt after this batch

- P1-01: scoped token rotation runbook transcript/evidence packaging
- P1-02: explicit replay-contention operator drill transcript/evidence packaging
- P1-05: cross-repo outage/recovery choreography drill bundle
- P1-06: final closeout packaging into release evidence/signoff chain
