{
  "module": "blackcat-core",
  "sourceCommit": "f1c3dc7",
  "status": "in progress",
  "requestPathProof": "rg -n \"libs/legacy/blackcat-core\" src",
  "primitiveGroups": [
    {
      "name": "byte helpers",
      "legacySymbols": ["readPositiveInteger", "utf8ByteLength", "bodyExceedsUtf8Limit"],
      "gatewayPaths": ["src/runtime/core/bytes.ts"],
      "tests": ["tests/runtime-core-bytes.test.ts"],
      "proof": "byte sizing and positive-integer parsing are gateway-owned and covered by focused tests"
    },
    {
      "name": "json parsing",
      "legacySymbols": ["parseJsonObject", "parseJsonArray", "parseJsonObjectBody", "parseJsonArrayBody"],
      "gatewayPaths": ["src/runtime/core/json.ts"],
      "tests": ["tests/runtime-core-json.test.ts"],
      "proof": "JSON shape and byte-limit checks are fail-closed in the gateway runtime"
    },
    {
      "name": "canonical json",
      "legacySymbols": ["canonicalizeJson"],
      "gatewayPaths": ["src/runtime/core/canonicalJson.ts"],
      "tests": ["tests/runtime-core-canonicalJson.test.ts"],
      "proof": "deterministic canonical JSON is used for integrity evidence and checkpoint signing"
    },
    {
      "name": "hash primitives",
      "legacySymbols": ["sha256Hex", "sha256Utf8", "hashJsonCanonical"],
      "gatewayPaths": ["src/runtime/core/hash.ts"],
      "tests": ["tests/runtime-core-hash.test.ts"],
      "proof": "deterministic SHA-256 helpers now live in the gateway runtime and are covered by known vectors plus canonical JSON parity tests"
    },
    {
      "name": "template helpers",
      "legacySymbols": ["template action guards", "template backend validation"],
      "gatewayPaths": ["src/runtime/template/actions.ts", "src/runtime/template/validators.ts"],
      "tests": ["tests/template-api.test.ts", "tests/validate-template-backend-contract.test.ts"],
      "proof": "template runtime remains gateway-owned and contract-checked before legacy removal"
    }
  ]
}
