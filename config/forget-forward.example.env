# Optional relay for successful local /cache/forget requests.
# Replace placeholder values with per-site worker values before deployment.

GATEWAY_FORGET_FORWARD_URL=https://worker-alpha.example.com/cache/forget
GATEWAY_FORGET_FORWARD_TOKEN=replace-with-forget-forward-token
GATEWAY_FORGET_FORWARD_TIMEOUT_MS=5000
